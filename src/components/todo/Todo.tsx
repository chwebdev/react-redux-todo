import TodoButton from './TodoButton'
import TodoList from './TodoList'

import React, { FC } from 'react'

const Todo: FC = () => {
	return (
		<>
			<TodoButton />
			<TodoList />
		</>
	)
}

export default Todo
